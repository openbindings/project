# OAS closeout proposed selection — 2026-09-10

This branch's `next.json` is an exact-source **qualification-only candidate**.
Its commits are saved repair/test branches, not claimed integration-line landings.
Do not merge these provisional pins unchanged: accepted development-line landing
must replace them with verified actual squash revisions. The shared `main`
candidate remains unchanged. Versions, release states, and candidate status are
preserved. No tag, package publication, release promotion, or deployment is implied.

The bounded pass closes N1 scalar-text correspondence and N2 other-root
classification in the unminted 3.2 binding, refreshes native authority, repairs
native scalar/header/reference and browser-host behavior, and corrects Go local
file URI handling. Core, published interface patterns, JSON/JSONata policy and
packages, and neutral CLI vocabulary are unchanged.

Evidence lives in the maintainer's `audit/oas-closeout-2026-09-10/run-2` record.
Native complete qualification and final authority delta review pass. The exact
remote Go adapter and complete TS SDK candidate gates pass. CLI local readonly
build/vet/race/conformance and executable journeys pass. Five real platform lanes,
normal-process browser/workbench, and this combined selection are under test.
The private website is qualified separately at its exact source; no public
cross-repository credential or deployment workaround is introduced.

The originally required historical held-out/acquisition seals have not been
located. Archived public probes and the small fresh public sample are not
substitutes. Therefore this record does not claim whole-ecosystem acceptance,
release readiness, a verified cohort, or permission to waive that evidence.
